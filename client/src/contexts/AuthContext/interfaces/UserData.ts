export interface UserData {
  // Add more fields as necessary
  uid: string;
  displayName: string;
  email: string;
  firstName: string;
  lastName: string;
  phoneNumber: string;
  photoUrl: string;
  rememberUser: boolean;
}
